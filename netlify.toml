[build]
  command = """
    npm ci &&
    npm run compile:migrations &&
    npm run migrate &&
    npm run build
  """
  publish   = "dist"
  functions = "netlify/functions"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
